plugins 
{
    id 'java'
}

group = 'com.github.francitoshi'
version = '0.1.14'
//remember to execute 'git tag -a x.x.x -m "Version x.x.x"'

repositories 
{
    mavenCentral()
    maven { url 'https://jitpack.io' }
}
//run "./gradlew dependencies --configuration runtimeClasspath" to verify version problems
dependencies
{
    implementation 'com.github.francitoshi:nut-base:0.1.14'
    implementation 'com.github.francitoshi:nut-core:0.1.14'

    compileOnly 'org.apache.commons:commons-compress:1.26.2'
   
    testImplementation 'org.junit.jupiter:junit-jupiter:5.10.3'
    testImplementation 'org.apache.commons:commons-csv:1.9.0'
}

java 
{
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
    withSourcesJar()
//    withJavadocJar()
}

wrapper 
{
    gradleVersion = "8.2"
    distributionType = Wrapper.DistributionType.ALL
}

test 
{
    useJUnitPlatform()
}
